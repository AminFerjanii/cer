<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Certificate</title>
  <style>
    /* Certificate styles */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f0f0f0;
    }
    .certificate {
      width: 800px;
      margin: 50px auto;
      padding: 30px;
      border: 10px solid #007bff;
      background-color: #fff;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
    }
    .title {
      text-align: center;
      font-size: 36px;
      margin-bottom: 20px;
      color: #007bff;
      text-transform: uppercase;
    }
    .content {
      font-size: 24px;
      margin-bottom: 30px;
      color: #333;
      text-align: center;
    }
    .logo {
      display: block;
      margin: 0 auto;
      width: 200px;
      height: auto;
    }
    .signature {
      display: block;
      margin: 20px auto 0;
      width: 150px;
      height: auto;
    }
    .footer {
      text-align: center;
      font-size: 16px;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="certificate">
    <img src="certificate-logo.png" alt="Certificate Logo" class="logo">
    <div class="title">Certificate of Completion</div>
    <div class="content">
      This is to certify that <strong>{{name}}</strong> has successfully completed the course <strong>{{course}}</strong>.
    </div>
    <img src="signature.png" alt="Signature" class="signature">
    <div class="footer">
      Issued on {{date}}
    </div>
  </div>
</body>
</html>
